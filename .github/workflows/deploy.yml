name: Deploy to AWS EC2

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Setup Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Build com Maven
      run: mvn clean package -DskipTests

    - name: Copiar JAR via SSH para EC2
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu
        key: ${{ secrets.EC2_SSH_KEY }}
        source: "target/teste-cicd-0.0.1-SNAPSHOT.jar"
        target: "/home/ubuntu/"

    - name: Rodar JAR via SSH na EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          echo "Parando aplicação existente, se houver..."
          pkill -f 'teste-cicd-0.0.1-SNAPSHOT.jar' || true
          echo "Subindo nova aplicação..."
          nohup java -jar /home/ubuntu/teste-cicd-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
          sleep 5
          echo "Verificando se a aplicação está respondendo..."
          if curl -s -o /dev/null -w "%{http_code}" http://localhost:8080 | grep -q "200"; then
            echo "✅ Aplicação no ar e respondendo com status 200!"
          else
            echo "❌ Aplicação não respondeu corretamente. Verifique os logs na EC2."
          fi
