name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK 21
        uses: actions/setup-java@v2
        with:
          java-version: '21'
          distribution: 'adopt'

      - name: Build with Maven
        run: mvn clean install -DskipTests

      - name: Build Docker image
        run: docker build -t teste-cicd:latest .

      - name: Print EC2 IP
        run: echo "EC2 IP: ${{ secrets.EC2_IP }}"

      - name: Ping EC2
        run: ping ${{ secrets.EC2_IP }} -c 4

      - name: SSH Deploy
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.EC2_KEY }}

      - name: SSH Commands
        run: |
          ssh -o StrictHostKeyChecking=no ec2-user@${{ secrets.EC2_IP }} '
            docker-compose down;
            docker-compose pull;
            docker-compose up -d
          '
